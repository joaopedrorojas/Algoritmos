#include <stdio.h>

int main() {
    
    int trilhas, medicao, anterior, atual;
    int indo = 0, voltando = 0;
    int indice = 0;

    scanf("%d", &trilhas);

    for(int i=0; i<trilhas; i++){

        scanf("%d", &medicao);

        scanf("%d", &anterior);
        for(int j=0; j<medicao; j++){
            scanf("%d", &atual);
            if(anterior > atual)
        }
        
    }

    return 0;
}